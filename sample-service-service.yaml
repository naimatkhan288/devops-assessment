# apiVersion: v1
# kind: Service
# metadata:
#   name: sample-service-service
# spec:
#   selector:
#     app: sample-service
#   ports:
#     #- protocol: TCP
#     - name: http
#       port: 80
#       nodePort: 30000
#   type: NodePort
#      # targetPort: 3000

 apiVersion: v1
 kind: Service
 metadata:
   name: sample-service-service
 spec:
   selector:
     app: sample-service
   ports:
     - protocol: TCP
       port: 80
       targetPort: 3000

